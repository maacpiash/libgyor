{
  "name": "libgyor",
  "npmClient": "yarn",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "populate": "node populate-books.js"
  },
  "dependencies": {
    "dotenv": "^8.0.0",
    "fs": "^0.0.1-security",
    "lerna": "^3.15.0"
  }
}
